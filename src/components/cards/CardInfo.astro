---
interface Props {
  title: string;
  description: string;
  imgSrc: string;
  altText: string;
}

const { title, description, imgSrc, altText } = Astro.props;
---
<div class="desplegable-card">
    